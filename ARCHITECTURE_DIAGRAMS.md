# 📊 Meeting System - Visual Summary

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     TEACHER FRONTEND                         │
│                  (React + Tailwind CSS)                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────┐          ┌──────────────────┐         │
│  │  App.js          │ Wrapped  │ TeacherProvider  │         │
│  │ (TeacherProvider)│ ────────>│  (teachapi.js)   │         │
│  └──────────────────┘          └──────────────────┘         │
│                                         │                    │
│                                         ▼                    │
│  ┌──────────────────────────────────────────────────┐       │
│  │       useTeacher() Hook - Global Context         │       │
│  ├──────────────────────────────────────────────────┤       │
│  │ • scheduleMeeting(data)                          │       │
│  │ • getMeetingsByCourse(courseId)                  │       │
│  │ • getStudentMeetings()                           │       │
│  │ • getMeetingById(meetingId)                      │       │
│  │ • cancelMeeting(meetingId, courseId)            │       │
│  │ • resendMeetingLink(meetingId, studentId)       │       │
│  │                                                  │       │
│  │ State: loading, error, success, meetings, etc   │       │
│  └──────────────────────────────────────────────────┘       │
│                                                               │
│  ┌──────────────────────────────────────────────────┐       │
│  │      Schedulemeet Component                      │       │
│  ├──────────────────────────────────────────────────┤       │
│  │ • Schedule Form                                  │       │
│  │ • Meetings List                                  │       │
│  │ • Meeting Details                                │       │
│  │ • Cancel & Resend Actions                        │       │
│  │ • Student Email Status                           │       │
│  └──────────────────────────────────────────────────┘       │
│                                                               │
└─────────────────────────────────────────────────────────────┘
                           │ AXIOS CALLS
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    BACKEND (Express/Node)                    │
│                   (API Routes & Logic)                       │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Route Layer (meetingRoutes.js)                              │
│  ├─ POST   /api/meetings/schedule                           │
│  ├─ GET    /api/meetings/course/:courseId                   │
│  ├─ GET    /api/meetings/student/my-meetings                │
│  ├─ GET    /api/meetings/:meetingId                         │
│  ├─ POST   /api/meetings/:meetingId/cancel                  │
│  └─ POST   /api/meetings/:meetingId/resend/:studentId       │
│                                                               │
│  ┌──────────────────────────────────────────────────┐       │
│  │  Controller Layer (meetingController.js)         │       │
│  ├──────────────────────────────────────────────────┤       │
│  │ ┌────────────────────────────────────────────┐  │       │
│  │ │ scheduleZoomMeeting                        │  │       │
│  │ │ • Validate inputs                          │  │       │
│  │ │ • Fetch enrolled students                  │  │       │
│  │ │ • Create Zoom meeting                      │  │       │
│  │ │ • Save to database                         │  │       │
│  │ │ • Send emails (async)                      │  │       │
│  │ └────────────────────────────────────────────┘  │       │
│  │ ┌────────────────────────────────────────────┐  │       │
│  │ │ getMeetingsByCourse                        │  │       │
│  │ │ cancelMeeting                              │  │       │
│  │ │ getStudentMeetings                         │  │       │
│  │ │ resendMeetingLink                          │  │       │
│  │ │ getMeetingById                             │  │       │
│  │ │ sendMeetingNotificationEmails (helper)     │  │       │
│  │ └────────────────────────────────────────────┘  │       │
│  └──────────────────────────────────────────────────┘       │
│                           │                                   │
│          ┌────────────────┼────────────────┐                │
│          ▼                ▼                ▼                │
│   ┌─────────────┐  ┌──────────────┐  ┌──────────────┐     │
│   │   Zoom      │  │   Email      │  │  Database    │     │
│   │   API       │  │   (Mailer)   │  │  (MongoDB)   │     │
│   └─────────────┘  └──────────────┘  └──────────────┘     │
│                                                               │
└─────────────────────────────────────────────────────────────┘
         │                    │                    │
         ▼                    ▼                    ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│  Zoom Servers    │ │  Gmail SMTP      │ │  MongoDB Atlas   │
│  - Meeting links │ │  - Email server  │ │  - Data storage  │
│  - Recording     │ │  - Credentials   │ │  - User/Course   │
└──────────────────┘ └──────────────────┘ └──────────────────┘
```

---

## Data Flow Diagram

### Scheduling a Meeting

```
┌─────────────────┐
│   Teacher       │
│   Fills Form    │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Schedulemeet Component             │
│  Form Validation                    │
│  - Topic required                   │
│  - Future date                      │
│  - 15-1440 minutes                  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  teachapi.js - scheduleMeeting()    │
│  - Validate again                   │
│  - Call backend API                 │
│  - Set loading state                │
│  - Handle errors                    │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Backend - scheduleZoomMeeting()    │
│  - Authenticate user (JWT)          │
│  - Validate inputs                  │
│  - Check course exists              │
│  - Fetch enrolled students          │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Zoom API                           │
│  - Create meeting                   │
│  - Get join_url                     │
│  - Get start_url                    │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Database - Save Meeting            │
│  {                                  │
│    topic, start_time, duration,     │
│    join_url, teacherId, courseId,   │
│    enrolledStudents: [...]          │
│  }                                  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Send Emails (async, non-blocking)  │
│  - Generate HTML template           │
│  - For each student:                │
│    • Send email with Zoom link      │
│    • Mark emailSent: true           │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Return Success Response            │
│  {                                  │
│    success: true,                   │
│    meeting: {...},                  │
│    emailsSent: 15,                  │
│    emailsFailed: 0                  │
│  }                                  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Frontend Updates                   │
│  - Show success message             │
│  - Add meeting to list              │
│  - Clear form                       │
│  - Refresh UI                       │
└─────────────────────────────────────┘
```

---

## File Relationship Diagram

```
┌──────────────────────────────────────────────────────────────┐
│                   TEACHER APP (React)                        │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  App.js                                                        │
│    │                                                           │
│    ├─> TeacherProvider (teachapi.js)                          │
│    │    │                                                     │
│    │    └─> useTeacher Hook                                  │
│    │         │                                               │
│    │         └─> API Calls to Backend                        │
│    │                                                          │
│    └─> Schedulemeet Component (uses useTeacher)              │
│         │                                                    │
│         ├─> Schedule Form                                   │
│         ├─> Meetings List                                   │
│         └─> Actions (Cancel, Resend)                        │
│                                                               │
└──────────────────────────────────────────────────────────────┘
                          │
                 ┌────────┴────────┐
                 │                 │
                 ▼                 ▼
         ┌───────────────┐ ┌───────────────┐
         │ Environment   │ │  localStorage │
         │ Variables     │ │  (JWT token)  │
         └───────────────┘ └───────────────┘


┌──────────────────────────────────────────────────────────────┐
│              BACKEND (Express/Node.js)                       │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  server.js                                                     │
│    │                                                           │
│    ├─> Routes (meetingRoutes.js)                              │
│    │    │                                                     │
│    │    └─> Protected by auth middleware                     │
│    │         │                                               │
│    │         └─> Controller (meetingController.js)           │
│    │              │                                          │
│    │              ├─> scheduleZoomMeeting()                 │
│    │              ├─> getMeetingsByCourse()                 │
│    │              ├─> getStudentMeetings()                  │
│    │              ├─> getMeetingById()                      │
│    │              ├─> cancelMeeting()                       │
│    │              ├─> resendMeetingLink()                   │
│    │              └─> sendMeetingNotificationEmails()       │
│    │                   │                                    │
│    │                   └─> (Helper Function)                │
│    │                                                         │
│    └─> External Services                                    │
│         │                                                   │
│         ├─> Zoom API (via zoomAuth.js token)                │
│         ├─> Email Service (mailer.js)                       │
│         └─> Database (Models)                               │
│              │                                              │
│              ├─> Meeting (meetingmodel.js)                  │
│              ├─> User (User.js - existing)                  │
│              └─> Course (CourseModel.js - existing)         │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## Component Interaction Flow

```
┌─────────────────────────────────────────────────────────────┐
│                         App.js                              │
│    ┌────────────────────────────────────────────┐          │
│    │    TeacherProvider from teachapi.js       │          │
│    │    Wraps entire app with context         │          │
│    └─────────────┬──────────────────────────────┘          │
│                  │                                          │
│                  ├─> useTeacher()                           │
│                  │   returns: {                             │
│                  │     scheduleMeeting(),                   │
│                  │     getMeetingsByCourse(),               │
│                  │     meetings,                            │
│                  │     loading,                             │
│                  │     error,                               │
│                  │     ...                                  │
│                  │   }                                      │
│                  │                                          │
│                  ▼                                          │
│    ┌────────────────────────────────────────┐             │
│    │    Schedulemeet Component              │             │
│    │                                        │             │
│    │  ┌──────────────────────────────────┐ │             │
│    │  │  Schedule Form                   │ │             │
│    │  │  - Input validation              │ │             │
│    │  │  - Submit → scheduleMeeting()    │ │             │
│    │  │  - Show success/error            │ │             │
│    │  └──────────────────────────────────┘ │             │
│    │                                        │             │
│    │  ┌──────────────────────────────────┐ │             │
│    │  │  Meetings List                   │ │             │
│    │  │  - Show meetings from context    │ │             │
│    │  │  - Display status                │ │             │
│    │  │  - Show student info             │ │             │
│    │  │  - Action buttons                │ │             │
│    │  │  - Cancel → cancelMeeting()      │ │             │
│    │  │  - Resend → resendMeetingLink()  │ │             │
│    │  └──────────────────────────────────┘ │             │
│    │                                        │             │
│    └────────────────────────────────────────┘             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ API Calls
                           │ (Axios)
                           ▼
        ┌──────────────────────────────────┐
        │    Backend API Routes            │
        │ /api/meetings/*                  │
        │ (Protected by JWT auth)          │
        └──────────────────────────────────┘
```

---

## State Management Flow

```
TeacherContext {
  │
  ├─ State
  │  ├─ loading: boolean         (API call in progress)
  │  ├─ error: string|null       (Error message if any)
  │  ├─ success: string|null     (Success message if any)
  │  ├─ meetings: []             (Teacher's course meetings)
  │  ├─ studentMeetings: []      (Student's meetings)
  │  └─ selectedMeeting: null    (Currently selected meeting)
  │
  ├─ Methods (useCallback hooks)
  │  ├─ scheduleMeeting(data)
  │  │  └─ POST /api/meetings/schedule
  │  │
  │  ├─ getMeetingsByCourse(courseId)
  │  │  └─ GET /api/meetings/course/:courseId
  │  │
  │  ├─ getStudentMeetings()
  │  │  └─ GET /api/meetings/student/my-meetings
  │  │
  │  ├─ getMeetingById(meetingId)
  │  │  └─ GET /api/meetings/:meetingId
  │  │
  │  ├─ cancelMeeting(meetingId, courseId)
  │  │  └─ POST /api/meetings/:meetingId/cancel
  │  │
  │  ├─ resendMeetingLink(meetingId, studentId)
  │  │  └─ POST /api/meetings/:meetingId/resend/:studentId
  │  │
  │  ├─ setError(msg)            (Set error state)
  │  ├─ setSuccess(msg)          (Set success state)
  │  └─ clearMessages()          (Clear after 5 seconds)
  │
  └─ Exported
     ├─ TeacherProvider          (Wrapper component)
     ├─ useTeacher()             (Hook to access context)
     └─ TeacherContext           (Context object)
```

---

## API Response Examples

### Schedule Meeting Response

```json
{
  "success": true,
  "message": "Meeting scheduled successfully",
  "meeting": {
    "_id": "507f1f77bcf86cd799439012",
    "topic": "Advanced Robotics Session",
    "start_time": "2025-09-20T15:00:00Z",
    "duration": 60,
    "join_url": "https://zoom.us/j/12345",
    "enrolledStudentsCount": 15,
    "emailsSent": 15,
    "emailsFailed": 0
  }
}
```

### Meeting Document in Database

```javascript
{
  _id: ObjectId("507f1f77bcf86cd799439012"),
  topic: "Advanced Robotics Session",
  description: "Building autonomous drones",
  start_time: Date("2025-09-20T15:00:00.000Z"),
  duration: 60,
  join_url: "https://zoom.us/j/12345",
  start_url: "https://zoom.us/s/12345",
  zoomMeetingId: "12345",
  teacherId: ObjectId("507f1f77bcf86cd799439010"),
  courseId: ObjectId("507f1f77bcf86cd799439011"),
  enrolledStudents: [
    {
      studentId: ObjectId("507f1f77bcf86cd799439013"),
      email: "student1@email.com",
      name: "John Doe",
      emailSent: true
    },
    {
      studentId: ObjectId("507f1f77bcf86cd799439014"),
      email: "student2@email.com",
      name: "Jane Smith",
      emailSent: true
    }
  ],
  status: "scheduled",
  createdAt: Date("2025-09-20T14:00:00.000Z"),
  updatedAt: Date("2025-09-20T14:00:00.000Z")
}
```

---

## Email Notification Structure

```
┌────────────────────────────────────────────┐
│          Meeting Scheduled Email           │
├────────────────────────────────────────────┤
│                                            │
│  Header: 🎓 Class Meeting Scheduled       │
│                                            │
│  Content:                                  │
│  ┌────────────────────────────────────┐   │
│  │ Hi [Student Name],                 │   │
│  │                                    │   │
│  │ Your teacher has scheduled a       │   │
│  │ class meeting for [Course Name]    │   │
│  │                                    │   │
│  │ Topic: [Meeting Topic]             │   │
│  │ Date & Time: [IST Timezone]        │   │
│  │ Duration: [Minutes] minutes        │   │
│  │ Description: [Optional]            │   │
│  │                                    │   │
│  │ [▶ Join Meeting Button]            │   │
│  │ (Links to Zoom)                    │   │
│  │                                    │   │
│  │ If you have issues, contact your   │   │
│  │ teacher.                           │   │
│  └────────────────────────────────────┘   │
│                                            │
│  Footer: © STEMelix                       │
│                                            │
└────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
User Action
    │
    ▼
Input Validation (Frontend)
    │
    ├─ Valid? ───► API Call
    │
    └─ Invalid? ──► Show Error Message
                   (Red alert box)
                        │
                        ▼
                   Auto-clear after 5s
                        │
                        ▼
                   User can retry


API Response
    │
    ├─ success: true ──► Update UI
    │                   Show success msg
    │                   Refresh meetings
    │
    └─ success: false ──► Show error msg
                         from response
                              │
                              ▼
                         User sees specific
                         error details
                         Auto-clear after 5s
```

---

## Security Layers

```
┌──────────────────────────────────────────────────────┐
│           Request Journey (Frontend → Backend)       │
├──────────────────────────────────────────────────────┤
│                                                       │
│  1. Frontend                                          │
│     ├─ Input Validation (client-side)               │
│     └─ Attach JWT Token to Header                   │
│        Authorization: Bearer [token]                │
│                                                      │
│  2. Network (HTTPS in production)                   │
│     └─ Encrypted transmission                       │
│                                                      │
│  3. Backend                                          │
│     ├─ protect() Middleware                         │
│     │  ├─ Verify JWT signature                      │
│     │  ├─ Check token expiry                        │
│     │  ├─ Fetch user from DB                        │
│     │  └─ Attach user to req.user                   │
│     │                                                │
│     ├─ Controller Validation                        │
│     │  ├─ Validate input fields                     │
│     │  ├─ Sanitize inputs                           │
│     │  ├─ Check authorization                       │
│     │  │  (e.g., only teacher can cancel)           │
│     │  └─ Verify resource exists                    │
│     │                                                │
│     └─ Database Query                               │
│        └─ Mongoose prevents SQL injection           │
│                                                      │
│  4. Response                                         │
│     ├─ Sensitive data removed                       │
│     └─ Error messages don't expose system info      │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

## Performance Optimization

```
┌─────────────────────────────────────────────────────┐
│           Performance Strategies                    │
├─────────────────────────────────────────────────────┤
│                                                     │
│ 1. Zoom Token Caching (zoomAuth.js)               │
│    ├─ Fetch token once                            │
│    ├─ Cache in memory                             │
│    ├─ Reuse until expiry - 60s                    │
│    └─ Result: Faster Zoom API calls               │
│                                                     │
│ 2. Database Queries                               │
│    ├─ Use .lean() for read operations             │
│    ├─ Select only needed fields                   │
│    ├─ Reduce network transfer                     │
│    └─ Result: Faster response times               │
│                                                     │
│ 3. Async Email Sending                            │
│    ├─ Send emails in background                   │
│    ├─ Don't wait for email completion             │
│    ├─ Return response immediately                 │
│    └─ Result: Fast API response                   │
│                                                     │
│ 4. React Context Optimization                     │
│    ├─ useCallback for methods                     │
│    ├─ Prevent unnecessary re-renders              │
│    ├─ Only update changed values                  │
│    └─ Result: Smooth UI updates                   │
│                                                     │
│ 5. Component Lazy Loading                         │
│    ├─ Load Schedulemeet only when needed          │
│    ├─ Don't include in initial bundle             │
│    ├─ Code-split for faster load                  │
│    └─ Result: Faster initial page load            │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    PRODUCTION SETUP                          │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌────────────────┐         ┌────────────────┐              │
│  │   Teacher App  │         │   Student App  │              │
│  │   (React)      │         │   (React)      │              │
│  └────────┬───────┘         └────────┬───────┘              │
│           │                         │                        │
│           └──────────┬──────────────┘                        │
│                      │                                       │
│                      ▼                                       │
│          ┌───────────────────────────┐                      │
│          │   Cloudflare/CDN          │                      │
│          │   (Caching, DDoS)         │                      │
│          └───────────┬───────────────┘                      │
│                      │                                       │
│                      ▼                                       │
│          ┌───────────────────────────┐                      │
│          │  API Gateway              │                      │
│          │  (Rate limiting)          │                      │
│          └───────────┬───────────────┘                      │
│                      │                                       │
│                      ▼                                       │
│          ┌───────────────────────────┐                      │
│          │  Backend Server (Node.js) │                      │
│          │  - Port 5000              │                      │
│          │  - Horizontal scaling     │                      │
│          │  - Load balancing         │                      │
│          └───────────┬───────────────┘                      │
│                      │                                       │
│          ┌───────────┼───────────────┐                      │
│          │           │               │                      │
│          ▼           ▼               ▼                      │
│      ┌────────┐  ┌────────┐  ┌──────────────┐              │
│      │MongoDB │  │Zoom    │  │Gmail SMTP    │              │
│      │(Atlas) │  │API     │  │(Email)       │              │
│      └────────┘  └────────┘  └──────────────┘              │
│                                                                │
└──────────────────────────────────────────────────────────────┘
```

---

**Visual architecture complete! 📊**

All diagrams show the complete system design and interactions.
